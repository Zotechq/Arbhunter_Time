# ArbHunter - Pre-Match Time-Discrepancy Hunter
Scrape bookmakers, detect kickoff time variations (stale bookies), log to CSV.

Setup: Install deps, run `tor`, then `python main.py`.
Focus: Time hunting (engine.py detect_variations).